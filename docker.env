# Set these to actual strong passwords in production!
# Leave REDIS_PASSWORD empty or set to "your-strong-password" to run Redis without password (for development)
REDIS_PASSWORD=your-strong-password
MYSQL_PASSWORD=another-strong-password